import LineGraph from '../../../../example/Regions/Multiline/MultipleLineRegions?dev-site-example';
import PropsTable from '@cerner/terra-graphs/lib/components/Linegraph?dev-site-props-table';
import DisplayData from '@cerner/terra-graphs/lib/components/helpers/DisplayData';

# Multiple line regions

This is timeseries graph with region associated to each line.
#### NOTE: Hover onto legend to see the region associated with each line.

## Getting Started

- Install with [npmjs](https://www.npmjs.com):
  - `npm install terra-graphs`

## Usage

```jsx
import React from 'react';
import LineGraph from '@cerner/terra-graphs/lib/components/LineGraph';
```
## Examples
<LineGraph />

## Data Used
<DisplayData graphID = { "#multipleline"} dataArrayNumber = {[0,2,4,3]} graphConfig = { 'LINE_DEFAULT' } />

## Props
<PropsTable />